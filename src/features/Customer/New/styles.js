import { StyleSheet } from "react-native";

const newStyles = () =>
  StyleSheet.create({
    container: {
      padding: 50,
    },
    auxText: {
      textAlign: "center",
      padding: 20,
      paddingTop: 0,
    },
  });

export { newStyles };
