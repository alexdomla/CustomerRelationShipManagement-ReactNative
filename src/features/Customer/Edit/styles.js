import { StyleSheet } from "react-native";

const editStyles = () =>
  StyleSheet.create({
    container: {
      padding: 50,
    },
    auxText: {
      textAlign: "center",
      padding: 20,
      paddingTop: 0,
    },
  });

export { editStyles };
