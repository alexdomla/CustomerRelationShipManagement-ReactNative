import New from "../../features/Customer/New";
import { SafeAreaView } from "react-native";

const NewScreen = () => {
  return (
    <SafeAreaView>
      <New />
    </SafeAreaView>
  );
};

export default NewScreen;
